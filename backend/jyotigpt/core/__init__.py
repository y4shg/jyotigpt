"""Core infrastructure: environment, settings, socket engine, pipes.

Submodules here intentionally import nothing from ``core`` itself at
package level, so each can be loaded independently (and in any order)
by the rest of the application.
"""
