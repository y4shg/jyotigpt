apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jyotigpt-ingress
  namespace: jyotigpt
  #annotations:
    # Use appropriate annotations for your Ingress controller, e.g., for NGINX:
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: jyotigpt.minikube.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jyotigpt-service
            port:
              number: 8080
